import styled from 'styled-components/native';
import {IContainer} from '../../models/Container';

export const Content = styled.View`
  flex: 1;
`;

export const Main = styled.SafeAreaView<IContainer>`
  flex: 1;
`;
